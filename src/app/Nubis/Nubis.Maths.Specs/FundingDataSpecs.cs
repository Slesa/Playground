using Machine.Specifications;
using Nubis.Maths.Model;

namespace Nubis.Maths.Specs
{
    /*[Subject(typeof (FundingData))]
    public class When_creating_funding_data
    {
        Establish context = () => _fundingData = new FundingData();
        Because of = () =>
            {
                _fundingData.Kreditsumme = 10000;
                _fundingData.Laufzeit = 144;
                _fundingData.NominalZins = 5.0m;
            };

        It should_have_kreditsumme = () => _fundingData.Kreditsumme.ShouldEqual(10000);
        It should_have_laufzeit = () => _fundingData.Laufzeit.ShouldEqual(144);
        It should_have_zinssatz = () => _fundingData.NominalZins.ShouldEqual(5);

        static FundingData _fundingData;
    }*/
}