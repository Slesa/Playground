<UserControl x:Class="Nubis.Funding.Views.FundingDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Resources="clr-namespace:Nubis.Funding.Resources" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition/>
        </Grid.RowDefinitions>
            
        <TextBlock Grid.Column="0" Grid.Row="0" Text="{x:Static Resources:Strings.FundingData_LabelAmount}" />
        <TextBox x:Name="inputAmount" Grid.Column="1" Grid.Row="0" />
        
        <TextBlock Grid.Column="0" Grid.Row="1" Text="{x:Static Resources:Strings.FundingData_LabelInterestRate}" />
        <TextBox x:Name="inputInterestRate" Grid.Column="1" Grid.Row="1" />

    </Grid>
</UserControl>
