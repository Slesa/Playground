<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Class="TwitterReader.MainWindow"
	x:Name="Window"
	Title="MainWindow"
	Width="640" Height="480">
	<Window.Resources>
		<DataTemplate x:Key="itemTemplate">
			<StackPanel>
				<TextBlock Text="{Binding XPath=title}"/>
			</StackPanel>
		</DataTemplate>
	</Window.Resources>

	<Grid x:Name="LayoutRoot" DataContext="{Binding Source={StaticResource rssDs}}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Button Content="Refresh" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="5"/>
		<TextBlock Text="{Binding XPath=/rss/channel/title}" Margin="5" Grid.Row="1" FontSize="16" Background="#FFFFc600" FontWeight="Bold" TextAlignment="Center"/>
		<ListView ItemTemplate="{DynamicResource itemTemplate}" ItemsSource="{Binding XPath=/rss/channel/item}" Margin="5" Grid.Row="2"/>
	</Grid>
</Window>