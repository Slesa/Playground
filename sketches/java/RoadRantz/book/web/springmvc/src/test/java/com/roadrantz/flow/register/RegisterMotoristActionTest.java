package com.roadrantz.flow.register;

import org.junit.Test;

public class RegisterMotoristActionTest
{
    @Test
    public void shouldPassMotoristFromFlowScopeToRantService() throws Exception
    {
        // MotoristFormObject motorist = new MotoristFormObject();
        // RequestContext requestContext = createMock(RequestContext.class);
        // MutableAttributeMap flowMap = createMock(MutableAttributeMap.class);
        // requestContext.getFlowScope();
        // expectLastCall().andReturn(flowMap);
        // flowMap.get("motoristForm");
        // expectLastCall().andReturn(motorist);
        // RantService rantService = createMock(RantService.class);
        // rantService.addMotorist(motorist.getMotorist());
        // expectLastCall();
        //
        // replay(requestContext);
        // replay(flowMap);
        // replay(rantService);
        //
        // RegisterMotoristAction registerMotoristAction = new RegisterMotoristAction();
        // registerMotoristAction.setRantService(rantService);
        // registerMotoristAction.execute(requestContext);
        //
        // verify(requestContext);
        // verify(flowMap);
        // verify(rantService);
    }
}
