package com.roadrantz.flow.register;

import org.junit.Test;

public class AddVehicleActionTest
{
    @Test
    public void shouldPushEmptyVehicleOnEndOfList() throws Exception
    {
        // MotoristFormObject motoristForm = new MotoristFormObject();
        // assertEquals(0, motoristForm.getScrubbedVehicles().size());
        //
        // RequestContext context = createMock(RequestContext.class);
        // MutableAttributeMap flowScope = createMock(MutableAttributeMap.class);
        // flowScope.get("motoristForm");
        // expectLastCall().andReturn(motoristForm);
        // context.getFlowScope();
        // expectLastCall().andReturn(flowScope);
        // replay(context);
        // replay(flowScope);
        //
        // AddVehicleAction action = new AddVehicleAction();
        // Event event = action.execute(context);
        // assertEquals("continue", event.getId());
        // assertEquals(1, motoristForm.getScrubbedVehicles().size());
    }
}
