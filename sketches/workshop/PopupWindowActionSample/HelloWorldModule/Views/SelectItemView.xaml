<UserControl x:Class="HelloWorldModule.Views.SelectItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="200" MinWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            
        <TextBlock Margin="2" Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="0">Please select an item: </TextBlock>
        <ListBox ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" Margin="2" Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="1"></ListBox>

        <Button Margin="2" Width="60" HorizontalAlignment="Right" Command="{Binding OkCommand}" Grid.Column="0" Grid.Row="2"> OK </Button>
        <Button Margin="2" Width="60" HorizontalAlignment="Right" Command="{Binding CancelCommand}" Grid.Column="1" Grid.Row="2"> Cancel </Button>

    </Grid>
</UserControl>
